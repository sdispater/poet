language: python

python:
  - "2.7"
  - "3.6"
  - "pypy"

before_install:
  - pip install pypoet
  - pip install codecov
  - poet install -v

script: py.test --cov=poet --cov-config=.coveragerc tests/

after_success:
  - codecov
